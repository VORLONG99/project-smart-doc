---
name: "project-smart-doc"
description: "维护项目文档的智能助手。用于初始化项目文档、提炼注意事项、生成提交概览、分析复杂逻辑、制定迁移/重构指南，并支持受控的技能升级。触发于用户明确请求（如“初始化文档”“更新注意事项”“总结本次改动”“分析核心逻辑”“生成迁移计划”“升级这个 skill”）或在有明确上下文的文档维护任务中。"
---

# Project Smart Documentation

以“低误触发、可执行、可复用”为目标维护项目文档。仅在用户明确提出文档维护请求或当前任务已经明确进入文档维护流程时执行。

## 输出目录与文件

- 使用 `doc/` 目录。
- 维护以下文件：
  - `doc/project_overview.md`
  - `doc/attention_points.md`
  - `doc/commit_history.md`
  - `doc/core_logic.md`
  - `doc/migration_guide.md` 或 `doc/refactoring_notes.md`
- 使用模板：`references/templates.md`

## 通用执行规则

1. 先检查上下文是否足以落盘；信息不足时先提问，不写猜测内容。
2. 优先增量更新，避免覆盖用户已有内容。
3. 对新增条目执行去重：
   - 默认唯一键：`标题 + 模块 + 日期`
   - 命中重复时改为更新原条目并标注“已更新”
4. 严格分离文档职责：
   - 业务注意事项仅写入 `attention_points.md`
   - 提交变更仅写入 `commit_history.md`
5. 如存在脚本，优先用脚本产出稳定结果；标记扫描使用 `scripts/scan_markers.py`。

## 能力 1: 初始化项目文档

### 触发

- 用户明确要求“初始化文档 / 分析项目 / 生成项目概览”。

### 执行

1. 识别项目类型、入口与关键配置（如 `package.json`, `requirements.txt`, `go.mod`, `.env.example`）。
2. 运行或模拟标记扫描，收集 `TODO/FIXME/HACK/NOTE/XXX/DEPRECATED`。
3. 按模板生成或更新 `doc/project_overview.md`，包含架构、技术栈、环境、目录职责、技术债务与风险。

## 能力 2: 捕获与更新注意事项

### 触发

- 用户明确要求记录“注意点/关键规则/坑点”。
- 用户在当前任务中给出明确业务约束或纠正说明，且要求“记下来”。

### 执行

1. 提炼规则：场景、约束、错误示例、正确做法。
2. 更新 `doc/attention_points.md`，必要时附相关文件路径。
3. 去重后追加或更新，写入日期与状态。

## 能力 3: 生成提交变更概览

### 触发

- 用户明确要求“总结改动/生成提交记录”，或
- 已有明确变更上下文且满足任一阈值：变更 > 100 行 或文件数 > 3。

### 执行

1. 分析 diff，归类为 `Feature/Fix/Refactor/Chore`。
2. 生成影响范围、潜在风险、验证点。
3. 仅写入 `doc/commit_history.md`。

## 能力 4: 深度逻辑分析

### 触发

- 用户明确要求“分析核心逻辑/复杂算法/实现原理”。

### 执行

1. 解释“为什么这么做”，不是只解释“做了什么”。
2. 给出边界条件、副作用、可替代方案（如有）。
3. 更新 `doc/core_logic.md`。

## 能力 5: 迁移与重构指南

### 触发

- 用户明确要求“迁移/重构/升级改造计划”。

### 执行

1. 对比现状与目标，列出差距。
2. 输出步骤化计划：准备、执行、回滚、验证。
3. 迁移写 `doc/migration_guide.md`，重构写 `doc/refactoring_notes.md`。

## 能力 6: 受控自我升级

### 触发

- 用户明确要求“升级这个 skill / 修改规则”。

### 执行

1. 先给出拟修改点摘要（触发、流程、模板、约束）。
2. 仅修改 `SKILL.md` 的规则正文，不改 `name`，谨慎改 `description`。
3. 在文件末尾追加成长日志，记录日期、改动与原因。

## 参考资源

- 文档模板：`references/templates.md`
- 标记扫描脚本：`scripts/scan_markers.py`

## 成长日志

> 记录 Skill 的自我进化历程。

- **[2026-02-10] 初始版本**: 实现了文档初始化、注意事项捕获、提交记录生成功能。
- **[2026-02-10] 功能增强**: 增加了复杂逻辑深度剖析和迁移/重构向导功能。
- **[2026-02-11] 能力觉醒**: 增加了 Self-Evolution 自我进化能力，允许动态修改规则。
- **[2026-02-24] 精简规则并强化可执行性**：收紧触发条件，增加去重规则，拆分模板到 `references/templates.md`，新增 `scan_markers.py` 脚本入口。
